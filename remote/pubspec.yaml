name: remote
description: Remote api layer.
version: 1.0.0
publish_to: 'none'
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.2 <4.0.0'

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  
  domain:
    path: ../domain

  supabase_flutter: ^1.10.1
  cross_file: ^0.3.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: